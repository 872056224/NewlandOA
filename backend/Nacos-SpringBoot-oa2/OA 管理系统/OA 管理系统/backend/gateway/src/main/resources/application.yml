server:
  port: 8888

spring:
  application:
    name: oa-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: localhost:8848
        file-extension: yml
        namespace: public
        group: DEFAULT_GROUP
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # 员工服务路由
        - id: emp-service
          uri: lb://oa-emp-service
          predicates:
            - Path=/api/v1/employee/**
        # 管理员服务路由
        - id: admin-service
          uri: lb://oa-admin-service
          predicates:
            - Path=/api/v1/admin/**
        # AI 客服服务路由（直连：AI 服务为 Boot 3.x 异构技术栈，
        # 暂不注册 Nacos 以规避新旧 nacos-client 协议兼容问题；后续可切换 lb://oa-ai-service）
        - id: ai-service
          uri: http://localhost:8083
          predicates:
            - Path=/api/v1/ai/**
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
            exposedHeaders: "*"

# 日志配置
logging:
  level:
    com.oa.gateway: debug
    org.springframework.cloud.gateway: debug
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n' 