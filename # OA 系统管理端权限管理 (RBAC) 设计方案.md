# OA 系统管理端权限管理 (RBAC) 设计方案

## 1. 现状分析与核心思路

目前系统中的 `admin`（管理员）表和 `emp`（员工）表是**完全独立**的。`admin` 表仅包含账号密码，缺乏组织架构上下文（部门、职务）。
为了实现基于组织架构的权限控制（如“本部门部长”、“人事部部长”），核心思路是：**将 `admin` 账号与 `emp` 员工信息进行绑定**。

通过关联员工的 `dept_id`（部门）和 `duty_id`（职务），动态推导并赋予管理员相应的操作权限和数据可见范围。

## 2. 角色判定逻辑与权限矩阵

基于现有的 `emp`、`department`、`duty` 表结构，设计如下角色判定逻辑：

### 👑 1. 董事长 (Chairman)
*   **判定条件**：关联的员工 `duty_id = 17`（需新增职务：董事长）。
*   **权限范围**：**最高全局权限**。
    *   可查看/审批所有部门的考勤、请假、补签。
    *   可制定和修改全局考勤规则。
    *   拥有全部人事任免权（包括人事部部长和其他部门部长）。

### 👔 2. 人事部部长 (HR Director)
*   **判定条件**：关联的员工 `dept_id = 1`（人事部） 且 `duty_id = 1`（部长）。
*   **权限范围**：**全局业务权限 + 受限的人事权**。
    *   可管理所有部门的考勤、请假、补签。
    *   可管理全局考勤规则。
    *   **人事权限制**：在进行人事任免/修改时，**无权**操作 `duty_id = 17`（董事长）和 `duty_id = 1`（各部门部长）的员工。

### 💼 3. 部门部长 / 副部长 (Dept Head / Deputy)
*   **判定条件**：关联的员工 `dept_id = X`（某部门） 且 `duty_id = 1`（部长）或 `2`（需新增职务：副部长）。
*   **权限范围**：**本部门数据隔离权限**。
    *   **业务权限**：只能查看和审批 `dept_id = X`（本部门）员工的请假和补签申请。
    *   **人事权限**：只能进行本部门的普通人事任免（不能操作董事长、其他部门员工，也不能修改自身职务）。
    *   **系统权限**：**无权**查看和修改全局考勤规则。

## 3. 数据库改造方案 (极简侵入)

为了支持上述逻辑，仅需对现有数据库进行微调：

1.  **`admin` 表改造**：
    *   新增字段 `emp_number` (int)，用于关联 `emp` 表的 `number` 主键。
2.  **`duty` 表数据补充**：
    *   插入新职务：`duty_id = 2`, `duty_name = '副部长'`
    *   插入新职务：`duty_id = 17`, `duty_name = '董事长'`

## 4. 前后端落地执行步骤

### Step 1: 后端登录与鉴权改造
1.  **实体类更新**：更新 `Admin` 实体类，增加 `empNumber` 字段，以及关联的 `deptId` 和 `dutyId` 属性。
2.  **登录逻辑修改**：管理员登录成功后，联表查询其对应的 `emp` 信息。
3.  **角色计算**：根据查出的 `deptId` 和 `dutyId`，计算出当前管理员的角色枚举（如 `CHAIRMAN`, `HR_DIRECTOR`, `DEPT_HEAD`），并存入 Session/Token。

### Step 2: 后端接口数据过滤 (Data Permission)
在各个业务 Controller/Service 中加入基于角色的逻辑判断：
*   **列表查询接口（请假、补签、员工列表）**：
    *   如果是 `DEPT_HEAD`，在 SQL 查询条件中强制追加 `WHERE dept_id = #{currentAdmin.deptId}`。
*   **操作类接口（审批、人事修改）**：
    *   如果是 `HR_DIRECTOR` 尝试修改员工，校验目标员工的 `duty_id`，若为 1 或 17 则抛出权限异常。
    *   如果是 `DEPT_HEAD` 尝试审批，校验目标单据的所属部门是否与自己一致。

### Step 3: 前端 UI 权限适配
1.  **状态管理**：登录后，将后端返回的 `roleType` 存入 Pinia/Vuex。
2.  **菜单控制**：根据 `roleType` 动态渲染左侧菜单（例如，`DEPT_HEAD` 隐藏“考勤规则管理”菜单）。
3.  **按钮级控制**：
    *   在员工列表页，如果是 `HR_DIRECTOR` 登录，渲染列表时判断行数据的 `duty_id`，若为董事长或部长，则禁用“编辑/删除”按钮。
    4.注意修改审批之后要对通知的范围做出改变，比如普通员工请假申请之后只会发给部门的部长和副部长以及董事长